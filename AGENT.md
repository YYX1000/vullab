# 🤖 AI Agent 核心准则

>
> 本项目为 vullab 漏洞复现靶场：`labs/` 下按漏洞类型 / 常见 CMS 分类存放 Docker Compose 环境，由 GitHub Actions（`ci.yml` 遍历构建校验、`release.yml` tag 发布）驱动闭环；处理本项目时，必须**严格遵守**以下规范。

## 1. 核心思维与行为红线

- **第一性原理与深度思考**：面对复杂需求或 Bug，禁止生搬硬套过往经验。必须剥离表象，回归基础事实进行逻辑推导，**放慢分析步伐，禁止跳跃式决策**。
- **无痕执行与安全红线（靶场强化）**
  - 主动创建的临时验证文件、临时靶场测试容器必须在任务结束后清理。
  - 涉及销毁容器、清理网络、清理数据卷、丢弃未追踪文件（如 `git reset --hard` / `git clean -fd`）等破坏性操作前，**必须向用户说明影响并获批**。
  - 禁止随意挂载宿主机根目录、系统敏感目录至容器。
- **防御性版本控制 (防误删机制)**：仅当用户明确输入 “WIP” 指令时，Agent **必须执行**`git add` 相关文件，并在当前分支进行阶段性提交（如 `git commit -m "WIP: xxx"`），确保阶段性成果进入本地版本控制；未明确要求时**不得自动触发**。严禁在有大量未提交代码时，直接建议或进行大范围的回退操作（如 `git restore`）。
- **整洁地提交记录 (Squash WIPs)**：为了防止频繁的 WIP 提交污染主分支的提交记录，在开发完成准备最终 Push 前，Agent **必须主动提醒并协助**用户使用 `git reset --soft` 合并（Squash）这些零碎的 WIP 提交，保持提交历史整洁。

## 2. 编码与架构规范

- **代码整洁与极简**：坚持 “少即是多”。避免过度设计，UI 最小化层级嵌套；函数只做一件事，优先保证人类可读性（禁止取巧写法）；优先使用标准库（最少依赖），并必须显式捕获与处理异常。
- 密钥、敏感配置全部通过环境变量注入，禁止硬编码写入代码、配置文件或镜像内；POC、EXP、测试脚本不得打包进生产镜像。

## 3. CI/CD 与部署闭环

### 3.1 开发工作流

1. 新建靶场：统一放在 `labs/<漏洞类型>/<靶场名>/`，每个靶场自带 `docker-compose.yml` 与 `README.md`（复现步骤 / PoC）。compose 文件名统一为 `docker-compose.yml`（CI 仅匹配该名称，`.yaml` 后缀不会被校验）；多环境 / 多版本靶场（如 ruoyi：官方 / Plus / 芋道 Pro × java8 / java17）用子目录隔离。
2. 注意：`labs/cms/` 存放的是**常见 CMS 完整环境**（如 ruoyi、seacms、wordpress 等），而非单纯漏洞靶场；各 CMS 目录同样要求 compose + README 自包含。
3. 新增 / 调整靶场后必须同步更新根目录 `README.md` 的靶场清单与目录说明。
4. 初步搭建：用最少的开发时间搭建漏洞环境，并验证环境搭建成功。
5. 网络暴露基线：仅暴露必要的前端端口，后端服务不对外映射，通过内部桥接网络互通（参照 `labs/xxe/CVE-2025-68493`：前端 80→8080，后端不暴露）。
6. 体积优化：在搭建的基础上极致压缩容器体积，开发完成后进行验证。
7. 项目收尾清理：清除代码内无用代码块、废弃调试片段、无效注释；再维护、优化生产侧 Docker 相关文件，采用多阶段构建完成容器镜像的极致体积压缩。

### 3.2 上传防污染机制

**必须强制排除跨平台虚拟环境与构建产物目录**（`.venv`、`node_modules`、`__pycache__`、`dist` 等，已维护进根目录 `.gitignore`），提交前用 `git status` 复核；严禁将 Mac/Windows 本地的环境包覆盖至远端 Linux 容器。

- 敏感配置约定：开发环境使用 `.env` 进行本地开发；**仅提交 `.env.example` 模板（含占位值），`.env` 不得入库**，避免密钥泄露。
- 本地环境配置（如 `proxy.txt`）不得打包进镜像或混入发布产物，避免破坏已启动的实例环境。

### 3.3 配置与性能

- 版本以 Git Tag（`vX.Y.Z`，semver）为单一来源：push tag 时 `.github/workflows/release.yml` 自动生成 GitHub Release（仓库根目录不存在 `version.txt`，不得依赖该文件）；发布前确认版本已 bump。
- 代理 `proxy.txt`：仅在本机网络不通、镜像 / 依赖拉取超时时作为代理兜底使用；提交 / 发布前确认其不被打包进镜像或混入产物。

### 3.4 发布流程

1. 修改必须同步至本地源码，本地验证受影响靶场 `docker compose up -d --build` 成功，验证后询问是否 `git commit`。
2. 靶场验收硬性标准：新增 / 修改靶场后必须本地 `docker compose up -d --build` 验证通过，并 `docker compose down -v` 清理，最终以 CI 校验为准。
3. Push / PR 到 `main` 前：检查相关 `Dockerfile` 是否符合多阶段构建规范；CI（`.github/workflows/ci.yml`）会遍历 `labs/` 下所有 `docker-compose.yml` 执行 `config / build / up / ps / logs / down` 校验。
4. CI 校验失败时不得以 `--no-verify` 绕过，须修复问题后重新推送。
5. Push 到 `origin` 前：询问是否需打 Tag（`v*`）触发 `release.yml` 自动构建 GitHub Release。
6. CI 构建靶场镜像，CI 环境容器禁止使用特权模式。

## 4. 文档与输出规范

- **全局语言**：生成的内容与 Artifacts 必须使用**中文**。
- **代码审查与扫描 (Review & Diff)**：审查严禁仅看局部，必须完整追溯调用链上下文；提炼变更特性时**绝对禁止**仅看 `git log/status`，必须通过导出完整差异（如 `git diff -U0`）深度审阅，挖掘底层重构，确保 100% 无死角不遗漏。
- **Git Commit**：必须采用**严谨、客观、专业**的工程化表述（如 “规避”、“收敛”）；提交信息尽量区分平台后端变更、靶场漏洞案例变更；严禁主观营销与情感词汇（如 “完美解决”、“彻底消除”），精确传达技术价值。
